# SoulX-Singer 操作指南

## 整曲与长音频

现在启动 SVS / SVC 后默认进入「整曲工作台」：支持完整音频、气口分段、逐段试听与重生成、断点恢复和伴奏拼接。SVS 可导入 TXT / DOC / DOCX 新歌词。原有功能保留在高级模式。

详见 [整曲工作台说明](docs/longform.md)。长歌请使用整曲工作台，不要在短片段入口截取整首歌。

## 启动方式（双击 .bat 文件）

| 文件 | 功能 |
|------|------|
| `启动_SVC_WebUI.bat` | 声音转换：用你的音色唱别人的歌 |
| `启动_SVS_WebUI.bat` | 歌声合成：从零生成歌声，可改歌词和音高 |
| `启动_MIDI编辑器.bat` | 编辑 MIDI（歌词/音高/节奏） |
| `检查环境.bat` | 检查本机 CUDA、模型与离线设置 |

---

## SVC vs SVS 区别

- **SVC**（声音转换）：两个音频输入 → 用你的音色唱 Target 的歌。简单快捷，不能改歌词。
- **SVS**（歌声合成）：音频 + MIDI/乐谱 → 从零合成。可以改歌词、改音高，更可控但流程复杂。

**小白首选 SVC。**

---

## SVC 操作流程

1. 打开 `启动_SVC_WebUI.bat`
2. 上传 **Prompt 音频**（你唱的一段，干声最好）
3. 上传 **Target 音频**（你想翻唱的歌，可以带伴奏）
4. 勾选「人声分离」（如果 Target 不是纯人声）
5. 设置参数（建议默认：n_steps=32, cfg=3.0）
6. 点 **🎤声音转换** → 等几分钟 → 下载 generated.wav

## SVC 参数说明

| 参数 | 作用 | 建议值 |
|------|------|--------|
| n_steps | 采样步数，越大质量越好但越慢 | 32 |
| cfg | 音色相似度，太大可能失真 | 1~3 |
| pitch_shift | 手动变调（半音） | 0 |
| auto_shift | 自动匹配音域 | 勾选 |

---

## SVS 操作流程

1. 打开 `启动_SVS_WebUI.bat`
2. 上传 **Prompt 音频**（参考人声，干声最好）
3. 上传 **Target 音频**（**必须有原唱的歌**，纯伴奏不行！）
4. 等转录完成 → 检查「Transcriptions & Metadata」区域的内容
5. （可选但推荐）下载 **Target MIDI** → 去编辑器改歌词/音高 → 重新上传
6. 点 **🎤歌声合成** → 等生成 → 下载 generated.wav

## SVS MIDI 编辑流程

1. 在 SVS WebUI 的「Target MIDI」位置下载 `.mid` 文件
2. 双击 `启动_MIDI编辑器.bat`
3. 导入 `.mid` 文件
4. 编辑：
   - 拖拽音符块：上下 = 改音高，左右 = 改时机，拖边缘 = 改时长
   - 双击音符：改歌词
   - 右侧列表：批量编辑歌词
   - 点时间尺 = 跳转播放位置
5. 导出 `.mid` 文件
6. 回到 SVS WebUI，在「Target MIDI」上传改好的文件
7. Metadata 自动更新，点「🎤歌声合成」重新生成

> Prompt MIDI **不需要编辑**，只调 Target 就行。

---

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| Target 转录无歌词 | Target 是纯伴奏 | 换有原唱的歌 |
| SVS 生成音质差 | 自动转录不准 | 用 MIDI 编辑器手动调 |
| SVC 音色不像 | Prompt 质量不好 | 用干净的干声，别用带混响的 |
| 启动报错 | 模型没下载 | 确保 `pretrained_models/SoulX-Singer/` 下有 model.pt 和 model-svc.pt |
| DLL 报错 | torchcodec 不兼容 | 已卸载，不影响 |
